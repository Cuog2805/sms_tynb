@model SMS_TYNB.ViewModel.BaseFormViewModel<SMS_TYNB.Models.Master.WpCanbo>
@{
	ViewBag.Title = "Quản lý liên hệ";
	Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
	<div class="row">
		<div class="card p-0">
			@* form *@
			<div id="data-form">
				@{
					await Html.RenderPartialAsync("_Form", Model);
				}
			</div>
		</div>
	</div>

	<div class="row">
		<div class="row d-flex justify-content-between my-2 ms-0 p-0">
			<div class="col-md-6 btn-list">
				<button type="button" id="btnAdd" class="btn btn-primary" onclick="beforeAdd()">Thêm</button>
				<button type="button" id="btnEdit" class="btn btn-primary" onclick="startEdit()" disabled>Sửa</button>
				<a class="btn btn-primary" type="button" asp-area="" asp-controller="Group" asp-action="Index">Thêm nhóm</a>
				<a class="btn btn-primary" type="button" asp-area="" asp-controller="Group" asp-action="Assign">Phân nhóm</a>
			</div>

			<div class="col-md-6 d-flex justify-content-end align-items-center">
				<label for="searchInput" class="form-label me-2 mb-0">Tìm kiếm:</label>
				<input type="text" class="form-control form-control-sm" id="searchInput" placeholder="Tìm kiếm theo tên hoặc sđt" style="width: 200px;">
			</div>
		</div>
		@* danh sách *@
		<table class="table table-bordered" id="contactTable">
			<thead class="table-primary text-white">
				<tr>
					<th class="text-center">STT</th>
					<th>Tên cán bộ</th>
					<th>Số điện thoại</th>
					<th>Giới tính</th>
					<th>Mô tả</th>
				</tr>
			</thead>
			<tbody id="contactTableBody">
			</tbody>
		</table>
	</div>
	@{
		await Html.RenderPartialAsync("_Pagination");
	}
</div>

@section Scripts {
	<script src="~/js/contract/contract.js"></script>
}